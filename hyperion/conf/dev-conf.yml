# Logger's levels.
logLevels:
  service: warn
  model: warn
  web: warn

# Server's configuration
server:
  staticPort: 80
  apiPort: 443  
  staticFolder: ./atlas/src

# SSL certificates. Do not specify them to use plain Http.
ssl:
  certificate: ./hyperion/conf/ssl/localhost.crt
  key: ./hyperion/conf/ssl/localhost.key

# MongoDB connection
mongo:
  host: localhost
  db: mythic-forge

# Executable scripts
executable:
  source: ../proto-game
  target: ./hyperion/lib/compiled

# Turns execution settings. Frequency in seconds
turn:
  frequency: 60