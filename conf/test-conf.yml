# Logger's levels.
logLevels:
  service: error
  model: error
  web: error

# Server's configuration
server:
  host: localhost
  staticPort: 3080
  apiPort: 3443  
  cookieSecret: Hey monkey donkey !

# Authentication details
authentication:
  success: 'http://localhost:3443/rheia/authenticated?'
  error: 'http://localhost:3443/rheia/authenticated?'  
  tokenLifeTime: 900 # number of seconds during wich tokens are valid
  inactivityTime: 600 # number of seconds of inactivity before kicking user
  # Keys usable on localhost domain without SSL
  google:
    id: 750195776526-hkrs2b3a7ueltmu10108r6j9kcii1elo.apps.googleusercontent.com
    secret: zXQImGPy5xrZeEp7qEdOFP0g
  twitter:
    id: LosYMjbW1mompu5Dxkg3Bw
    secret: COQe0t413fAcMRlU5eK7ZthPxolV5nf2tfs442lZUU

# SSL certificates. Do not specify them to use plain Http.

# MongoDB connection
mongo:
  host: localhost
  db: mythic-forge-tests

# Executable scripts
executable:
  source: ./hyperion/lib/game-test/rules
  target: ./hyperion/lib/compiled-test

# Game client
game:
  # Must not contains or be a sibling of other paths.
  dev: ./hyperion/lib/game-test/source/client
  optimized: ./hyperion/lib/game-test/optimized
  production: ./hyperion/lib/game-test/final
  save: ./hyperion/lib/game-test/previous

# Turns execution settings. Frequency in seconds
turn:
  frequency: 5

# Images file storage
images:
  store: ./hyperion/lib/images-test