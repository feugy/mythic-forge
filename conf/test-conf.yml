# Logger's levels.
logLevels:
  service: warn
  model: warn
  web: warn

# SSL certificates. Do not specify them to use plain Http.

# MongoDB connection
mongo:
  host: localhost
  db: mythic-forge-tests

# Executable scripts
executable:
  source: D:/Programmation/Workspace2/mythic-forge-proto/lib/game-test
  target: D:/Programmation/Workspace2/mythic-forge-proto/lib/compiled-test

# Turns execution settings. Frequency in seconds
turn:
  frequency: 5